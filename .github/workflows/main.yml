name: "testing creating actions"

on:
  pull_request: 
    types: [opened]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: hello
        uses: ./
       
